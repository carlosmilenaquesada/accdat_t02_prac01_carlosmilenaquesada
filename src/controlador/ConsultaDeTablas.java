package controlador;

public class ConsultaDeTablas {
    //En esta clase se guardan las funciones que se usarán contra las tablas
    //de la base de datos, tales como los query 'select' construidos por 
    //el usuario.
}
